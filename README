npx create-next-app --typescript
npx sb init --type react
npm install storybook-dark-mode storybook-tailwind-dark-mode

npm install -D tailwindcss
:: далее для сборки css запускаем:
:: npx tailwindcss -i ./styles/tailwind.config.css -o ./styles/tailwind.dist.css --watch
