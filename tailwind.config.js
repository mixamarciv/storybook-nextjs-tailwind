module.exports = {
    content: ['./components/*.{ts,tsx}', './pages/*.{ts,tsx}'],
    darkMode: 'media', // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
